{
  "name": "readmeforge",
  "private": true,
  "version": "0.1.0",
  "description": "Local-first AI README generator for developers",
  "license": "MIT",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev -w apps/web",
    "build": "npm run build -w packages/core && npm run build -w apps/web && npm run build -w apps/cli",
    "test": "npm run test -w packages/core",
    "lint": "eslint . --ext .ts,.tsx",
    "cli": "node apps/cli/dist/index.js"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  },
  "engines": {
    "node": ">=18"
  }
}
